id: Hybrid_Comm_Step_Gate
label: Step Gate
category: '[Hybrid Communication]'

templates:
  imports: import Hybrid_Comm
  make: Hybrid_Comm.Step_Gate(${packetSize}, ${levels}, ${hopPoints})
  callbacks:
  - set_PacketSize(${packetSize})
  - set_Levels(${levels})
  - set_HopPoints(${hopPoints})


#  Make one 'parameters' list entry for every parameter you want settable from the GUI.
#     Keys include:
#     * id (makes the value accessible as \$keyname, e.g. in the make entry)
#     * label (label shown in the GUI)
#     * dtype (e.g. int, float, complex, byte, short, xxx_vector, ...)
parameters:
- id: packetSize
  label: Input packet samples size
  dtype: int
  default: 1000
- id: levels
  label: Output levels
  dtype: raw
  default: (-1, 0, +1)
- id: hopPoints
  label: Hopping points
  dtype: raw
  default: (-1, +1)


asserts:
  - ${ packetSize >= 1 }
  - ${ len(levels) == len(hopPoints) + 1 }


#  Make one 'inputs' list entry per input and one 'outputs' list entry per output.
#  Keys include:
#      * label (an identifier for the GUI)
#      * domain (optional - stream or message. Default is stream)
#      * dtype (e.g. int, float, complex, byte, short, xxx_vector, ...)
#      * vlen (optional - data stream vector length. Default is 1)
#      * optional (optional - set to 1 for optional inputs. Default is 0)
inputs:
- label: sig 1
  dtype: float
- label: sig 2
  dtype: float

outputs:
- label: sel
  dtype: float


documentation: |-
  The block maps the difference between two input signals to a piece-wise step value.
  If the inputs are 'Sig 1' and 'Sig 2', then output = f(Sig 1 - Sig 2).
  The relation between the output and 'Sig 1 - Sig 2' is based on piece-wise step function.  


#  'file_format' specifies the version of the GRC yml format used in the file
#  and should usually not be changed.
file_format: 1
