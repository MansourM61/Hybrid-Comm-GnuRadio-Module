id: FSO_Comm_Geometric_Loss
label: Geometric Loss
category: '[FSO Communication]'

templates:
  imports: import FSO_Comm
  make: FSO_Comm.Geometric_Loss(${diaTx}, ${thetaTx}, ${diaRx}, ${linkLen})
  callbacks:
  - set_DiaTx(${diaTx})
  - set_ThetaTx(${thetaTx})
  - set_LinkLen(${linkLen})
  - set_DiaRx(${diaRx})

#  Make one 'parameters' list entry for every parameter you want settable from the GUI.
#     Keys include:
#     * id (makes the value accessible as \$keyname, e.g. in the make entry)
#     * label (label shown in the GUI)
#     * dtype (e.g. int, float, complex, byte, short, xxx_vector, ...)
parameters:
- id: diaTx
  label: Tx aperture diameter (mm)
  dtype: float
  default: 5.0
- id: thetaTx
  label: Tx beam full-divergence angle (Deg)
  dtype: float
  default: 0.1
- id: diaRx
  label: Rx aperture diameter (mm)
  dtype: float
  default: 50.0
- id: linkLen
  label: Link length (m)
  dtype: float
  default: 100.0


asserts:
  - ${ diaTx >= 0 }
  - ${ thetaTx >= 0 }
  - ${ diaRx > 0 }
  - ${ linkLen > 0 }


#  Make one 'inputs' list entry per input and one 'outputs' list entry per output.
#  Keys include:
#      * label (an identifier for the GUI)
#      * domain (optional - stream or message. Default is stream)
#      * dtype (e.g. int, float, complex, byte, short, xxx_vector, ...)
#      * vlen (optional - data stream vector length. Default is 1)
#      * optional (optional - set to 1 for optional inputs. Default is 0)
inputs:
- label: in
  dtype: float

outputs:
- label: out
  dtype: float
- label: loss
  dtype: float
  optional: 1


#  'file_format' specifies the version of the GRC yml format used in the file
#  and should usually not be changed.
file_format: 1
